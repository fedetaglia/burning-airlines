<%= form_for(@airplane) do |f| %>
  <% if @airplane.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@airplane.errors.count, "error") %> prohibited this airplane from being saved:</h2>

      <ul>
      <% @airplane.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, id:'new-name', :class => 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :rows %><br>
    <%= f.number_field :rows, id:'new-rows', :class => 'form-control'  %>
  </div>
  <div class="form-group">
    <%= f.label :columns %><br>
    <%= f.text_field :columns, id:'new-columns', :class => 'form-control'  %>
  </div>
  <div class="actions">
    <%= f.submit id:'create-new', :class => 'btn btn-warning' %>
  </div>
<% end %>
